# 📷 手机摄像头访问指南

## ❓ 为什么手机上摄像头不能用？

**原因：** 现代浏览器的安全策略要求使用 **HTTPS** 才能访问摄像头（除了localhost）。

当您通过 `http://192.168.2.124:3000` 访问时，这是不安全的HTTP连接，浏览器会**阻止摄像头访问**。

---

## ✅ 解决方案

### 方案1：使用手动输入（推荐 - 最简单）

**步骤：**
1. 打开：`http://192.168.2.124:3000/mobile.html`
2. 点击"Scanner QR Code"
3. 选择Table和Position
4. **直接在输入框输入件号**
5. 点击"✓ Valider"

**优点：**
- ✅ 无需配置
- ✅ 任何网络都能用
- ✅ 不需要摄像头权限
- ✅ 快速可靠

---

### 方案2：使用HTTPS（推荐 - 完整功能）

**步骤1：** HTTPS服务器已启动在端口3443

**步骤2：** 在手机浏览器访问：
```
https://192.168.2.124:3443/mobile.html
```

**步骤3：** 处理安全警告

#### iOS Safari:
1. 看到"此网站不安全"
2. 点击**"显示详细信息"**
3. 点击**"访问此网站"**
4. 确认

#### Android Chrome:
1. 看到"您的连接不是私密连接"
2. 点击**"高级"**
3. 点击**"继续访问 192.168.2.124（不安全）"**

**步骤4：** 授权摄像头
1. 点击"Scanner QR Code"
2. 点击"📷 Scanner avec caméra"
3. 浏览器提示"允许访问摄像头"
4. 点击**"允许"** ✓

**现在摄像头可以正常使用了！**

---

## 🔍 详细说明

### 为什么需要HTTPS？

```
浏览器安全策略:
├─ localhost           → ✅ 允许摄像头 (开发环境)
├─ https://任何域名    → ✅ 允许摄像头 (安全连接)
└─ http://IP地址       → ❌ 禁止摄像头 (不安全连接)
```

### 访问对比表

| 地址 | 协议 | 摄像头 | 手动输入 | 推荐场景 |
|------|------|--------|---------|---------|
| `http://localhost:3000` | HTTP | ✅ | ✅ | 电脑本地测试 |
| `http://192.168.2.124:3000` | HTTP | ❌ | ✅ | 手机快速测试 |
| `https://192.168.2.124:3443` | HTTPS | ✅ | ✅ | 手机完整功能 |

---

## 🛠️ 服务器状态

### 检查服务器是否运行

```powershell
# 查看Node进程
Get-Process | Where-Object {$_.ProcessName -eq "node"}
```

### 启动服务器

```powershell
# HTTP服务器 (端口 3000)
npm start

# HTTPS服务器 (端口 3443)
node server-https.js
```

### 同时运行两个服务器

```powershell
# 在两个PowerShell窗口分别运行
窗口1: npm start
窗口2: node server-https.js
```

---

## 📱 测试清单

### HTTP版本 (http://192.168.2.124:3000)
- [x] 页面加载
- [x] 底部导航
- [x] 手动输入工作
- [ ] 摄像头不可用（预期行为）

### HTTPS版本 (https://192.168.2.124:3443)
- [ ] 页面加载
- [ ] 跳过安全警告
- [ ] 手动输入工作
- [ ] 摄像头授权
- [ ] 摄像头扫描QR码

---

## 🔧 故障排除

### 问题1: "此网站无法访问"

**检查：**
```powershell
# 确认服务器运行
Get-Process node

# 确认端口监听
netstat -an | findstr "3000"
netstat -an | findstr "3443"
```

**解决：**
```powershell
# 重启服务器
cd "c:\source\LAT SCANNER INVENTAIRE"
npm start
# 或
node server-https.js
```

### 问题2: "摄像头权限被拒绝"

**iOS Safari:**
1. 设置 > Safari > 网站设置
2. 找到 192.168.2.124
3. 相机 > 允许

**Android Chrome:**
1. 设置 > 网站设置 > 摄像头
2. 找到 192.168.2.124
3. 允许

### 问题3: "安全警告无法跳过"

**原因：** 某些企业浏览器禁止自签名证书

**解决：**
1. 使用手动输入（方案1）
2. 或使用ngrok等隧道服务获取真实HTTPS

---

## 🌐 获取真实HTTPS（可选）

### 使用ngrok

```bash
# 安装ngrok
# https://ngrok.com/download

# 启动隧道
ngrok http 3000

# 获得类似这样的URL:
# https://abc123.ngrok.io
```

**优点：**
- ✅ 真实的HTTPS证书
- ✅ 无安全警告
- ✅ 可从任何地方访问

**缺点：**
- ❌ 需要注册ngrok账号
- ❌ 依赖互联网连接

---

## 📊 推荐使用方案

### 场景1: 快速开发测试
👉 **HTTP + 手动输入**
```
http://192.168.2.124:3000/mobile.html
直接输入件号，快速验证功能
```

### 场景2: 完整功能测试
👉 **HTTPS + 摄像头**
```
https://192.168.2.124:3443/mobile.html
跳过安全警告，测试扫描功能
```

### 场景3: 实际生产环境
👉 **真实HTTPS域名**
```
部署到云服务器，配置SSL证书
例: https://scanner.yourcompany.com
```

---

## 💡 开发建议

### UI已优化
- ✅ 手动输入作为主要方式（更突出）
- ✅ 摄像头作为备选方式
- ✅ 清晰的错误提示
- ✅ HTTPS要求说明

### 用户体验
大多数用户在生产环境中会：
1. **首选手动输入** - 更快更可靠
2. **偶尔使用摄像头** - 当手动输入不便时

因此当前UI设计是合理的：
- 手动输入框在上方（突出）
- 摄像头按钮在下方（备选）

---

## 📞 快速参考

| 需求 | 使用 | 地址 |
|-----|------|------|
| 电脑测试 | HTTP | http://localhost:3000/mobile.html |
| 手机快速测试 | HTTP + 手动输入 | http://192.168.2.124:3000/mobile.html |
| 手机摄像头测试 | HTTPS | https://192.168.2.124:3443/mobile.html |

---

## ✨ 总结

**最佳实践：**
1. 开发时使用HTTP + 手动输入（简单快速）
2. 测试摄像头时使用HTTPS（完整功能）
3. 生产环境部署到真实域名（用户友好）

**当前状态：**
- ✅ HTTP服务器运行在 3000 端口
- ✅ HTTPS服务器运行在 3443 端口
- ✅ UI优化：手动输入为主，摄像头为辅
- ✅ 友好的错误提示和引导

**现在就可以开始测试了！** 🚀
